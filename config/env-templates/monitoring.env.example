# Monitoring Stack Configuration Template
# Copy this file to system/monitoring/.env and fill in secure values
#
# Generate secure passwords using:
#   openssl rand -base64 32
#   or
#   head -c 32 /dev/urandom | base64

# Grafana Admin Password
# Used to login to Grafana web interface (http://homelab-01:3001)
GRAFANA_ADMIN_PASSWORD=<CHANGE_ME_SECURE_PASSWORD>

# Security Notes:
# - Use a strong, unique password
# - Minimum 16 characters recommended
# - Store password securely (password manager)
# - Never commit this file to version control after filling in values
