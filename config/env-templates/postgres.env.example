# PostgreSQL Configuration Template
# Copy this file to platform/postgres/.env and fill in secure values
#
# Generate secure passwords using:
#   openssl rand -base64 32
#   or
#   head -c 32 /dev/urandom | base64

# PostgreSQL Admin Credentials
# Used by the postgres container for the superuser account
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=<CHANGE_ME_SECURE_PASSWORD>

# PgAdmin Configuration
# Email used to login to PgAdmin web interface (http://homelab-01:5050)
PGADMIN_EMAIL=admin@example.com

# PgAdmin password for web interface login
PGADMIN_PASSWORD=<CHANGE_ME_SECURE_PASSWORD>

# Security Notes:
# - Use different passwords for each service
# - Minimum 20 characters recommended
# - Store passwords securely (password manager)
# - Never commit this file to version control after filling in values
