# Gitea Configuration Template
# Copy this file to platform/gitea/.env and fill in secure values
#
# Prerequisites:
# 1. PostgreSQL must be running
# 2. Database and user must be created (see platform/gitea/README.md)
#
# Generate secure passwords using:
#   openssl rand -base64 32

# Database Configuration
# Type of database (postgres, mysql, sqlite3)
GITEA_DB_TYPE=postgres

# Database connection string
# Format: <container-name>:<port>
# The container name 'postgres' is from platform/postgres/docker-compose.yml
GITEA_DB_HOST=postgres:5432

# Database name - must match the database created in PostgreSQL
GITEA_DB_NAME=gitea

# Database user - must match the user created in PostgreSQL
GITEA_DB_USER=gitea

# Database password - must match the password set when creating the user
GITEA_DB_PASSWD=<CHANGE_ME_SECURE_PASSWORD>

# Setup Instructions:
# 1. Ensure PostgreSQL is running
# 2. Create the database and user:
#    docker exec -it postgres psql -U postgres
#    CREATE DATABASE gitea;
#    CREATE USER gitea WITH ENCRYPTED PASSWORD 'your-password-here';
#    GRANT ALL PRIVILEGES ON DATABASE gitea TO gitea;
#    ALTER DATABASE gitea OWNER TO gitea;
# 3. Copy this file to platform/gitea/.env
# 4. Update GITEA_DB_PASSWD with the password you set
# 5. Start Gitea: cd platform/gitea && docker compose up -d

# Security Notes:
# - Use a strong, unique password (20+ characters)
# - This password is stored in plain text - protect this file
# - Never commit this file to version control after filling in values
